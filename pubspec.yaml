name: kdigital_test
description: Kdigital test application

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.7.0
  flutter_bloc: ^8.0.1
  equatable: ^2.0.3
  http: ^1.2.2
  json_annotation: ^4.4.0
  cupertino_icons: ^1.0.2
  dio: ^5.7.0
  injectable: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.12
  json_serializable: ^6.8.0
  flutter_lints: ^3.0.0
  injectable_generator: ^2.6.2

flutter:
  uses-material-design: true
